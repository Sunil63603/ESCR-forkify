//contains all the variables(that we will be using through out the project).

export const API_KEY = `cd355376-a6b2-4306-b38f-e3374c20dde8`;
export const API_URL = `https://forkify-api.herokuapp.com/api/v2/recipes`;
export const TIMEOUT_SECONDS = 10;
export const RES_PER_PAGE = 10;
export const MODAL_CLOSE_SEC = 2.5;

//try to implement this logic
/*
export const totalRecipes = state.searchResults.totalResults;
export const totalPages = Math.ceil(state.searchResults.totalResults/10);
*/